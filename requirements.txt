# ─────────────────────────────────────────────────────────────────────────────
# RAG University Assistant — Python Dependencies (Serverless / Vercel)
# Version : 2.0.0
#
# Changement majeur v2.0 :
#   PyTorch, sentence-transformers et tout l'écosystème Hugging Face local
#   ont été supprimés. L'embedding est désormais externalisé vers l'API
#   Hugging Face Inference (même modèle all-MiniLM-L6-v2, 384 dims).
#   → Taille estimée du déploiement : ~30 Mo (vs ~2 Go avant)
#
# Install :
#   python3 -m venv venv
#   source venv/bin/activate        # macOS/Linux
#   venv\Scripts\activate           # Windows
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.128.8
uvicorn==0.39.0                   # local dev uniquement (Vercel n'utilise pas uvicorn)
starlette==0.49.3
python-multipart==0.0.20          # requis pour multipart/form-data (upload PDF)

# ── Data validation ───────────────────────────────────────────────────────────
pydantic==2.13.4
pydantic_core==2.46.4
annotated-types==0.7.0
typing_extensions==4.16.0
typing-inspection==0.4.2
annotated-doc==0.0.4

# ── MongoDB ───────────────────────────────────────────────────────────────────
pymongo==4.17.0
dnspython==2.7.0                  # requis par pymongo pour les connexions SRV Atlas

# ── PDF processing ────────────────────────────────────────────────────────────
PyMuPDF==1.26.5                   # extraction texte PDF (route /upload_course)
PyPDF2==3.0.1                     # scripts hors-ligne (extract_data.py)

# ── HTTP client (appels vers l'API Hugging Face Inference) ────────────────────
requests==2.32.5
certifi==2026.6.17
urllib3==2.6.3
charset-normalizer==3.4.9
idna==3.18

# ── Async / HTTP framework ────────────────────────────────────────────────────
anyio==4.12.1
h11==0.16.0
click==8.1.8
exceptiongroup==1.3.1

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv==1.2.1
