# ─────────────────────────────────────────────────────────────
# RAG University Assistant — Python Dependencies (Minimal)
#
# Install:
#   python -m venv venv
#   venv\Scripts\activate             # Windows
#   source venv/bin/activate          # macOS/Linux
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Web framework ─────────────────────────────────────────────
fastapi
uvicorn
python-multipart             # required for PDF upload (multipart/form-data)

# ── MongoDB ───────────────────────────────────────────────────
pymongo
dnspython                    # required by pymongo for SRV connection strings

# ── Embeddings (query vectorization for $vectorSearch) ────────
sentence-transformers        # loads all-MiniLM-L6-v2 to encode queries → 384d vectors
                             # NOTE: torch must be installed first (CPU-only recommended)

# ── PDF text extraction ───────────────────────────────────────
pymupdf                      # runtime extraction via fitz in /upload_course
pypdf2                       # offline extraction script (extract_data.py)

# ── Environment ───────────────────────────────────────────────
python-dotenv                # loads .env file (MONGODB_URI)
